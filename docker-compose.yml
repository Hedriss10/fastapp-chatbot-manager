version: '3.8'

services:  
  fastapi:
      build: .
      container_name: fastapi_app
      ports:
        - "8000:8000"
      volumes:
        - ./app:/app/app
      env_file:
        - .env
      restart: unless-stopped

volumes:
  fastapp_db:

networks:
  evolution-net:
    driver: bridge